services:
  # react-template.preview:
  #   image: react-template-server:latest
  #   restart: always
  #   ports:
  #     - 127.0.0.1:3000:8080
  #   environment:
  #     - CONNECTION_STRING=Host=react-template.db;Port=5432;Database=react-template;Username=postgres;Password=postgres
  #     - OTEL_EXPORTER_OTLP_ENDPOINT=http://react-template.aspire-dashboard:18889
  #     - OTEL_EXPORTER_OTLP_PROTOCOL=grpc
  #   depends_on:
  #     - react-template.db
  #   volumes:
  #     - ./ReactTemplate.Server/keys/storage:/app/keys/storage

  react-template.db:
    image: postgres
    restart: always
    user: postgres
    volumes:
      - react-template.db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432
    expose:
      - 5432
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

  react-template.aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:9.3
    environment:
      DOTNET_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS: true
    ports:
      - 18888:18888

volumes:
  react-template.db-data:
